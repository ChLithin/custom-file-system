1) ls [option] [directory]
	
	common option : -l --- long listing formats
	4 input combinations possible
2) cd [directory]
	3 input combinations possible
	(cd .. ----> back to previoes directory)
3) cp <source> <destination_path>
	1 combinaton
4) mv <source> <destination_path>
	2 combination
	even for rename just write new name instead of path
5) rm [option] <file>
	common options : -r ----> recursively remove directories
	-f -----> force delete without confirmation
6) touch <file>
7) cat <file>
8) mkdir <name>
9)rmdir <name>
10) search [option] <label>
	options : -r ----> search in all directries from current
		-o -----> only in current directory
11)filter [option] <extention>
	option : -e -----> filter by encryption status  in current directory(only option is needed)
		-r -----> filter with extention in current directory(option along with extention needed)
12) status <file_name>
	tells the info about encryption if file exist in current directory
		
13) encrypt <filename> <passkey>
	encrypts the file in current directory
14) decrypt <filename> <passkey>
15) addlabel <file name> <label>
	2 cases folder,file
16) space
17) del // to delete account and data related to them
